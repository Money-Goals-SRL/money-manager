{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend of webstite",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "react": "npm start --prefix react",
    "dev": "concurrently \"npm run start\" \"npm run react\"",
    "build": "cd ../frontend && npm install && npm run build && cd ../backend && rm -rf docs && cp -R ../frontend/build docs"
  },
  "author": "Leonardo Pinna",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^2.0.21"
  }
}
